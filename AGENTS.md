# 两账号协作规则

用户明确选择按癌种分工，不按功能模块分工。当前 A=BRCA，B 癌种待指定。阅读 coordination/assignments.tsv；任何未分配癌种不得自行开跑，先向用户询问一次。

## 科学边界

- 冻结 CAMP Phase1 原效应、P/q、分类不覆盖、不悄悄重算。确认实质错误时记录影响并另建修正版。
- 新的癌种内直接关系、患者关联、功能证据分析属于用户已授权的当前研究阶段；旧项目 AGENTS.md 的 Phase1-only/禁止候选研究描述是旧阶段限制。本仓库保留其数据驻留与样本身份规则。
- 先复用已有统计、映射和来源；不再做全包审核作为开工前置条件。
- 关联只在对应肿瘤标本中做。作者标本ID唯一不证明患者独立；不要按编号/排序推断配对。重复区域/患者须用合适独立单位，无法确认就明确限制。
- 统计键为队列×实际代谢特征×基因×分析版本。多个反应出处不重复检验。不同峰不因同名就合并。
- 原 CAMP q、新 RNA q、新代谢物-RNA q 分开。BH 范围预先固定；增加关系时另建版本，不照抄旧 q。后续同数据复核不称独立验证。
- 不新增未经说明的变换、填补或批次校正。相同输入和方法复用统计值；改变方法/样本则记录新版本。
- 患者关联显著不是功能筛选入场门槛。全候选批量取证，重点候选采用患者支持与适用功能支持的并集，不仅保留 BRCA 的10个。
- 分开 DONE、NOT_RUN、NOT_EVALUABLE、ACCESS_BLOCKED；缺资料不是阴性，文献命中不是功能证明。
- 已有 CPTAC BRCA 肿瘤-正常比较有组织/批次混杂，不作为支持候选的显著性分数。
- 跨癌方向对照可以早做；一个癌种显著另一个不显著不证明癌种差异。独立性按患者来源，不按网站和论文计数。

## 数据与并发

- 源矩阵、患者级测量、源压缩包和凭据不得进 GitHub 或本地交接包；只存 server165 的既有 data/candidates 或明确的新子目录。不得下载原始质谱。
- 每个癌种仅一名写入负责人。服务器结果必须使用新路径 `results/collaborative/<CANCER>/<ACCOUNT>/<RUN_ID>/`，脚本也复制到对应运行目录后执行；不得直接运行会覆盖历史目录的脚本。
- 不共享可写工作树；各电脑各自 clone。同一服务器运行目录建立独占 `.running` 锁，已有锁不覆盖；不删除另一账号的锁或终止其进程。
- 禁止修改另一癌种结果。公共映射/代码通过 PR 提交并列明影响；不得顺手重算其他癌种。
- 每次运行保存输入哈希、代码提交号、参数、版本、实际 n、检验范围和不可评估原因。数值完成后再写 DONE，失败保存错误。
- GitHub 分支不是服务器文件隔离；两者都要做。仓库不包含个人账号记忆；项目上下文以版本化文档交接。

向用户用中文说明结果的意义、完成项与未完成项，不把 rho/g 当倍数，不把 RNA/蛋白丰度当酶活或通量。
